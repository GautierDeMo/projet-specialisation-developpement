// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Looking for ways to speed up your queries, or scale easily with your serverless or edge functions?
// Try Prisma Accelerate: https://pris.ly/cli/accelerate-init

generator client {
  provider = "prisma-client"
  output   = "../generated/prisma"
}

datasource db {
  provider = "postgresql"
}

// TODO: Ne pas oublier le `prisma migrate dev` dès lors que le(s) schéma(s) sont écrits
// Ainsi que la génération du client avec `npx prisma generate`
// https://www.prisma.io/docs/getting-started/prisma-orm/quickstart/postgresq
