// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Looking for ways to speed up your queries, or scale easily with your serverless or edge functions?
// Try Prisma Accelerate: https://pris.ly/cli/accelerate-init

generator client {
  provider = "prisma-client"
  output   = "../generated/prisma"
}

datasource db {
  provider = "postgresql"
}

model Image {
  id Int @id @default(autoincrement())

  data      Bytes?
  productId Int     @map("product_id")
  url       String?

  product Product @relation(fields: [productId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now()) @map("created_at")
  updatedAt DateTime @default(now()) @updatedAt @map("updated_at")
}

model Product {
  id Int @id @default(autoincrement())

  category    String
  description String
  images      Image[]
  name        String  @unique
  price       Float

  createdAt DateTime @default(now()) @map("created_at")
  updatedAt DateTime @default(now()) @updatedAt @map("updated_at")
}

model Token {
  id Int @id @default(autoincrement())

  expiredAt DateTime @map("expired_at")
  token     String
  userId    Int      @map("user_id")

  user User @relation(fields: [userId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now()) @map("created_at")
  updatedAt DateTime @default(now()) @updatedAt @map("updated_at")
}

model User {
  id Int @id @default(autoincrement())

  email    String  @unique
  password String
  token    Token[]

  createdAt DateTime @default(now()) @map("created_at")
  updatedAt DateTime @default(now()) @updatedAt @map("updated_at")
}

model CspReport {
  id Int @id @default(autoincrement())

  documentUri       String @map("document_uri")
  violatedDirective String @map("violated_directive")
  blockedUri        String @map("blocked_uri")
  rawReport         String @map("raw_report") @db.Text

  createdAt DateTime @default(now()) @map("created_at")

  @@map("csp_reports")
}

// Ne pas oublier le `(npx) prisma migrate dev` dès lors que le(s) schéma(s) sont écrits
// Ainsi que la génération du client avec `(npx) prisma generate`
// https://www.prisma.io/docs/getting-started/prisma-orm/quickstart/postgresq
